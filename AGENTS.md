# Development rules

Run `npm test` and `npm run build` before delivery. The server owns rules, hidden state and scores; NPCs only use public observations. Keep original characters and existing approved pixel portraits. Do not claim planned games or emotional behavior are implemented.

Never commit environment secrets, browser credentials or game data. Update `HANDOFF.md` and `CHANGELOG.md` after substantive changes.
